<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>IA y Derechos Fundamentales</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: #333;
            overflow-x: hidden;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        /* Header */
        .header {
            text-align: center;
            padding: 40px 0;
            animation: fadeInDown 1s ease-out;
        }

        .header h1 {
            font-size: 3.5rem;
            color: white;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
            margin-bottom: 20px;
            animation: bounceIn 1.2s ease-out;
        }

        .authors {
            background: rgba(255,255,255,0.9);
            border-radius: 25px;
            padding: 20px;
            margin: 20px auto;
            max-width: 600px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            animation: slideInUp 1s ease-out 0.5s both;
        }

        .authors h3 {
            color: #4a90e2;
            margin-bottom: 15px;
            font-size: 1.3rem;
        }

        /* Slide container */
        .slide-container {
            position: relative;
            min-height: 70vh;
            margin: 40px 0;
        }

        .slide {
            display: none;
            background: white;
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 15px 35px rgba(0,0,0,0.1);
            animation: slideIn 0.6s ease-out;
            position: relative;
            overflow: hidden;
        }

        .slide.active {
            display: block;
        }

        .slide::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 5px;
            background: linear-gradient(90deg, #ff6b6b, #4ecdc4, #45b7d1, #96ceb4);
            border-radius: 20px 20px 0 0;
        }

        .slide h2 {
            color: #2c3e50;
            font-size: 2.5rem;
            margin-bottom: 30px;
            text-align: center;
            position: relative;
        }

        .slide h2::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            width: 100px;
            height: 4px;
            background: linear-gradient(90deg, #ff6b6b, #4ecdc4);
            border-radius: 2px;
        }

        /* Definition card */
        .definition-card {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border-radius: 15px;
            padding: 30px;
            margin: 20px 0;
            transform: translateY(20px);
            opacity: 0;
            animation: slideInCard 0.8s ease-out 0.3s both;
        }

        /* Marco jurídico sections */
        .marco-section {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
            margin: 30px 0;
        }

        .marco-card {
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            color: white;
            padding: 25px;
            border-radius: 15px;
            transform: scale(0.9);
            opacity: 0;
            animation: popIn 0.6s ease-out 0.4s both;
            cursor: pointer;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .marco-card:hover {
            transform: scale(1.05);
            box-shadow: 0 20px 40px rgba(0,0,0,0.2);
        }

        .marco-card h3 {
            font-size: 1.5rem;
            margin-bottom: 15px;
            text-align: center;
        }

        .marco-card ul {
            list-style: none;
        }

        .marco-card li {
            padding: 8px 0;
            padding-left: 20px;
            position: relative;
        }

        .marco-card li::before {
            content: '✓';
            position: absolute;
            left: 0;
            color: #fff;
            font-weight: bold;
        }

        /* Impact grid */
        .impact-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
            margin: 30px 0;
        }

        .impact-item {
            background: linear-gradient(135deg, #a8edea 0%, #fed6e3 100%);
            padding: 25px;
            border-radius: 15px;
            transform: translateY(30px);
            opacity: 0;
            animation: slideInStagger 0.6s ease-out both;
            border-left: 5px solid #ff6b6b;
            transition: all 0.3s ease;
        }

        .impact-item:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 30px rgba(0,0,0,0.1);
        }

        .impact-item:nth-child(1) { animation-delay: 0.1s; }
        .impact-item:nth-child(2) { animation-delay: 0.2s; }
        .impact-item:nth-child(3) { animation-delay: 0.3s; }
        .impact-item:nth-child(4) { animation-delay: 0.4s; }
        .impact-item:nth-child(5) { animation-delay: 0.5s; }
        .impact-item:nth-child(6) { animation-delay: 0.6s; }
        .impact-item:nth-child(7) { animation-delay: 0.7s; }

        .impact-item h4 {
            color: #2c3e50;
            font-size: 1.3rem;
            margin-bottom: 10px;
        }

        /* Rights list */
        .rights-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin: 30px 0;
        }

        .right-item {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 25px;
            border-radius: 15px;
            text-align: center;
            transform: rotateY(90deg);
            opacity: 0;
            animation: flipIn 0.8s ease-out both;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .right-item:hover {
            transform: rotateY(0deg) scale(1.05);
        }

        .right-item:nth-child(1) { animation-delay: 0.1s; }
        .right-item:nth-child(2) { animation-delay: 0.2s; }
        .right-item:nth-child(3) { animation-delay: 0.3s; }
        .right-item:nth-child(4) { animation-delay: 0.4s; }

        .right-number {
            font-size: 2rem;
            font-weight: bold;
            margin-bottom: 15px;
            color: #ffd93d;
        }

        /* Navigation */
        .navigation {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin: 40px 0;
            position: fixed;
            bottom: 20px;
            left: 50%;
            transform: translateX(-50%);
            background: rgba(255,255,255,0.9);
            padding: 15px 25px;
            border-radius: 50px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            z-index: 1000;
        }

        .nav-btn {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            padding: 12px 25px;
            border-radius: 25px;
            cursor: pointer;
            font-weight: bold;
            transition: all 0.3s ease;
            font-size: 1rem;
        }

        .nav-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.2);
        }

        .nav-btn:disabled {
            opacity: 0.5;
            cursor: not-allowed;
            transform: none;
            box-shadow: none;
        }

        .slide-counter {
            background: #2c3e50;
            color: white;
            padding: 8px 15px;
            border-radius: 20px;
            font-weight: bold;
            align-self: center;
        }

        /* Progress bar */
        .progress-container {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: rgba(255,255,255,0.3);
            z-index: 1001;
        }

        .progress-bar {
            height: 100%;
            background: linear-gradient(90deg, #ff6b6b, #4ecdc4, #45b7d1, #96ceb4);
            width: 0%;
            transition: width 0.5s ease;
        }

        /* Conclusion */
        .conclusion {
            background: linear-gradient(135deg, #ff9a9e 0%, #fecfef 100%);
            padding: 30px;
            border-radius: 20px;
            text-align: center;
            margin: 30px 0;
            transform: scale(0.8);
            opacity: 0;
            animation: zoomIn 0.8s ease-out 0.5s both;
        }

        .conclusion h3 {
            color: #2c3e50;
            font-size: 1.5rem;
            margin-bottom: 15px;
        }

        .conclusion p {
            font-size: 1.1rem;
            line-height: 1.6;
            color: #555;
        }

        /* Animations */
        @keyframes fadeInDown {
            from { opacity: 0; transform: translateY(-50px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes bounceIn {
            0% { transform: scale(0.3); opacity: 0; }
            50% { transform: scale(1.05); }
            70% { transform: scale(0.9); }
            100% { transform: scale(1); opacity: 1; }
        }

        @keyframes slideInUp {
            from { opacity: 0; transform: translateY(50px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes slideIn {
            from { opacity: 0; transform: translateX(100px); }
            to { opacity: 1; transform: translateX(0); }
        }

        @keyframes slideInCard {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes popIn {
            from { opacity: 0; transform: scale(0.8); }
            to { opacity: 1; transform: scale(1); }
        }

        @keyframes slideInStagger {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes flipIn {
            from { opacity: 0; transform: rotateY(90deg); }
            to { opacity: 1; transform: rotateY(0deg); }
        }

        @keyframes zoomIn {
            from { opacity: 0; transform: scale(0.8); }
            to { opacity: 1; transform: scale(1); }
        }

        /* Responsive */
        @media (max-width: 768px) {
            .header h1 { font-size: 2.5rem; }
            .slide h2 { font-size: 2rem; }
            .marco-section { grid-template-columns: 1fr; }
            .navigation { 
                position: relative; 
                transform: none;
                left: auto;
                bottom: auto;
            }
            .slide { padding: 20px; }
            .impact-grid { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>
    <div class="progress-container">
        <div class="progress-bar" id="progressBar"></div>
    </div>

    <div class="container">
        <div class="header">
            <h1>🤖 Inteligencia Artificial y Derechos Fundamentales</h1>
            <div class="authors">
                <h3>👥 Equipo de Investigación</h3>
                <p><strong>Viviana Pulgarín Marín</strong> • <strong>Vanessa Martinez</strong> • <strong>Madeleyn Velásquez</strong> • <strong>Ramiro Rincón</strong> • <strong>Sebastian Florez</strong></p>
            </div>
        </div>

        <div class="slide-container">
            <!-- Slide 1: Sentencia T-323 -->
            <div class="slide active">
                <h2>⚖️ Sentencia T-323</h2>
                <div class="definition-card">
                    <h3>🏛️ Corte Constitucional de Colombia</h3>
                    <p style="font-size: 1.3rem; margin-bottom: 20px;"><strong>Sobre el uso de IA por jueces de la República</strong></p>
                    <div style="text-align: center; margin: 30px 0;">
                        <div style="font-size: 4rem; margin-bottom: 20px;">🤖⚖️🇨🇴</div>
                    </div>
                    <p style="font-size: 1.1rem; line-height: 1.6;">Esta sentencia establece precedentes fundamentales sobre cómo los jueces pueden y deben utilizar herramientas de inteligencia artificial en el ejercicio de sus funciones jurisdiccionales, marcando un hito en la jurisprudencia nacional sobre tecnología y justicia.</p>
                </div>
                <div style="background: rgba(255,255,255,0.9); border-radius: 15px; padding: 20px; margin-top: 30px; border-left: 5px solid #ff6b6b;">
                    <p style="color: #2c3e50; font-weight: bold; text-align: center;">📋 Marco regulatorio para la implementación responsable de IA en el sistema judicial colombiano</p>
                </div>
            </div>

            <!-- Slide 2: Definición -->
            <div class="slide">
                <h2>🎯 Definición del Tema</h2>
                <div class="definition-card">
                    <h3>¿Qué es la Inteligencia Artificial?</h3>
                    <p>La IA comprende sistemas capaces de procesar datos y tomar decisiones con autonomía. Su expansión plantea retos jurídicos y éticos al afectar el ejercicio y garantía de los derechos fundamentales, en especial la dignidad humana, la igualdad y la privacidad.</p>
                </div>
                <div style="text-align: center; margin-top: 40px;">
                    <div style="font-size: 4rem; margin-bottom: 20px;">⚖️🤝🔒</div>
                    <p style="font-size: 1.2rem; color: #666;">Equilibrando innovación tecnológica con protección de derechos</p>
                </div>
            </div>

            <!-- Slide 2: Marco Jurídico Internacional -->
            <div class="slide">
                <h2>🌍 Marco Jurídico Internacional</h2>
                <div class="marco-section">
                    <div class="marco-card">
                        <h3>🇪🇺 Derecho Europeo</h3>
                        <ul>
                            <li>Reglamento General de Protección de Datos (RGPD)</li>
                            <li>Legislación contra la discriminación</li>
                            <li>Carta de los Derechos Fundamentales de la UE</li>
                        </ul>
                    </div>
                    <div class="marco-card">
                        <h3>🌐 Instrumentos Internacionales</h3>
                        <ul>
                            <li>Declaración Universal de los Derechos Humanos</li>
                            <li>Convenciones de la ONU</li>
                            <li>Convención Europea de Derechos Humanos</li>
                        </ul>
                    </div>
                </div>
            </div>

            <!-- Slide 3: Marco Jurídico Nacional -->
            <div class="slide">
                <h2>🇨🇴 Marco Jurídico Nacional</h2>
                <div class="marco-section">
                    <div class="marco-card">
                        <h3>📜 Constitución Política</h3>
                        <ul>
                            <li>Debido proceso (Art. 29)</li>
                            <li>Protección de datos</li>
                            <li>Dignidad humana</li>
                        </ul>
                    </div>
                    <div class="marco-card">
                        <h3>⚖️ Leyes y Jurisprudencia</h3>
                        <ul>
                            <li>Leyes Estatutarias 1266 de 2008 y 1581 de 2012</li>
                            <li>Jurisprudencia constitucional sobre IA</li>
                            <li>Criterios para uso judicial</li>
                        </ul>
                    </div>
                </div>
            </div>

            <!-- Slide 4: Impacto en el Ámbito Jurídico -->
            <div class="slide">
                <h2>⚡ Impacto en el Ámbito Jurídico</h2>
                <div class="impact-grid">
                    <div class="impact-item">
                        <h4>🔐 Privacidad</h4>
                        <p>Uso masivo de datos personales sin consentimiento.</p>
                    </div>
                    <div class="impact-item">
                        <h4>⚖️ Igualdad</h4>
                        <p>Algoritmos que reproducen sesgos y discriminación.</p>
                    </div>
                    <div class="impact-item">
                        <h4>🗣️ Libertad de Expresión</h4>
                        <p>Riesgo de censura o difusión de contenidos dañinos.</p>
                    </div>
                    <div class="impact-item">
                        <h4>💼 Trabajo</h4>
                        <p>Automatización que afecta estabilidad y acceso al empleo.</p>
                    </div>
                    <div class="impact-item">
                        <h4>🧬 Bioética</h4>
                        <p>Dilemas en salud, genética y dignidad humana.</p>
                    </div>
                    <div class="impact-item">
                        <h4>🛡️ Seguridad</h4>
                        <p>Vigilancia y armas autónomas con riesgos éticos y legales.</p>
                    </div>
                    <div class="impact-item">
                        <h4>🏛️ Debido Proceso</h4>
                        <p>Sistemas opacos que afectan transparencia, imparcialidad y defensa.</p>
                    </div>
                </div>
            </div>

            <!-- Slide 5: Derechos Fundamentales en Riesgo -->
            <div class="slide">
                <h2>🚨 Principales Derechos Fundamentales en Riesgo</h2>
                <div class="rights-container">
                    <div class="right-item">
                        <div class="right-number">01</div>
                        <h4>Derecho a la No Discriminación</h4>
                        <p>Algoritmos sesgados pueden perpetuar discriminación</p>
                    </div>
                    <div class="right-item">
                        <div class="right-number">02</div>
                        <h4>Derecho a la Protección de Datos y Privacidad</h4>
                        <p>Recolección masiva de información personal</p>
                    </div>
                    <div class="right-item">
                        <div class="right-number">03</div>
                        <h4>Derecho al Acceso a la Justicia</h4>
                        <p>Barreras tecnológicas en sistemas judiciales</p>
                    </div>
                    <div class="right-item">
                        <div class="right-number">04</div>
                        <h4>Derecho al Debido Proceso</h4>
                        <p>Falta de transparencia en decisiones automatizadas</p>
                    </div>
                </div>
            </div>

            <!-- Slide 6: Futuro de la IA -->
            <div class="slide">
                <h2>🚀 El Futuro de la Inteligencia Artificial</h2>
                <div style="text-align: center; margin: 40px 0;">
                    <div style="font-size: 5rem; margin-bottom: 30px;">🤖🔮</div>
                </div>
                <div class="definition-card">
                    <h3>Perspectivas y Desafíos</h3>
                    <p>El desarrollo futuro de la IA requiere un equilibrio cuidadoso entre la innovación tecnológica y la protección de los derechos fundamentales. Es esencial establecer marcos regulatorios adaptativos que fomenten el desarrollo responsable mientras salvaguardan los valores democráticos y los derechos humanos.</p>
                </div>
                <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 20px; margin-top: 30px;">
                    <div style="background: linear-gradient(135deg, #a8edea 0%, #fed6e3 100%); padding: 20px; border-radius: 15px; text-align: center;">
                        <div style="font-size: 2rem; margin-bottom: 10px;">🛡️</div>
                        <strong>Regulación Proactiva</strong>
                    </div>
                    <div style="background: linear-gradient(135deg, #ffecd2 0%, #fcb69f 100%); padding: 20px; border-radius: 15px; text-align: center;">
                        <div style="font-size: 2rem; margin-bottom: 10px;">🤝</div>
                        <strong>Colaboración Global</strong>
                    </div>
                    <div style="background: linear-gradient(135deg, #a8edea 0%, #fed6e3 100%); padding: 20px; border-radius: 15px; text-align: center;">
                        <div style="font-size: 2rem; margin-bottom: 10px;">📚</div>
                        <strong>Educación Digital</strong>
                    </div>
                </div>
            </div>

            <!-- Slide 7: Conclusiones -->
            <div class="slide">
                <h2>💡 Conclusiones</h2>
                <div class="conclusion">
                    <h3>Reflexión Final - Madeleyn Velásquez</h3>
                    <p>"La IA ofrece oportunidades extraordinarias para mejorar la sociedad, pero exige regulación cuidadosa y salvaguardias robustas que garanticen la protección de los derechos fundamentales en el entorno digital."</p>
                </div>
                <div style="text-align: center; margin-top: 40px;">
                    <div style="font-size: 4rem; margin-bottom: 20px;">🌟</div>
                    <h3 style="color: #2c3e50; margin-bottom: 20px;">¡Muchas Gracias!</h3>
                    <p style="font-size: 1.2rem; color: #666;">Por su atención y participación</p>
                </div>
            </div>
        </div>

        <div class="navigation">
            <button class="nav-btn" id="prevBtn" onclick="changeSlide(-1)">← Anterior</button>
            <div class="slide-counter">
                <span id="currentSlide">1</span> / <span id="totalSlides">7</span>
            </div>
            <button class="nav-btn" id="nextBtn" onclick="changeSlide(1)">Siguiente →</button>
        </div>
    </div>

    <script>
        let currentSlideIndex = 0;
        const slides = document.querySelectorAll('.slide');
        const totalSlides = slides.length;
        const progressBar = document.getElementById('progressBar');
        const currentSlideSpan = document.getElementById('currentSlide');
        const totalSlidesSpan = document.getElementById('totalSlides');
        const prevBtn = document.getElementById('prevBtn');
        const nextBtn = document.getElementById('nextBtn');

        // Initialize
        totalSlidesSpan.textContent = totalSlides;
        updateSlide();

        function showSlide(index) {
            slides.forEach((slide, i) => {
                slide.classList.toggle('active', i === index);
                if (i === index) {
                    // Trigger animations for current slide
                    slide.style.animation = 'none';
                    slide.offsetHeight; // Trigger reflow
                    slide.style.animation = 'slideIn 0.6s ease-out';
                }
            });
        }

        function updateSlide() {
            showSlide(currentSlideIndex);
            currentSlideSpan.textContent = currentSlideIndex + 1;
            
            // Update progress bar
            const progress = ((currentSlideIndex + 1) / totalSlides) * 100;
            progressBar.style.width = progress + '%';
            
            // Update navigation buttons
            prevBtn.disabled = currentSlideIndex === 0;
            nextBtn.disabled = currentSlideIndex === totalSlides - 1;
            
            if (currentSlideIndex === totalSlides - 1) {
                nextBtn.textContent = 'Fin 🎉';
            } else {
                nextBtn.textContent = 'Siguiente →';
            }
        }

        function changeSlide(direction) {
            const newIndex = currentSlideIndex + direction;
            if (newIndex >= 0 && newIndex < totalSlides) {
                currentSlideIndex = newIndex;
                updateSlide();
                
                // Add bounce effect to navigation
                const btn = direction > 0 ? nextBtn : prevBtn;
                btn.style.transform = 'translateY(-3px) scale(0.95)';
                setTimeout(() => {
                    btn.style.transform = 'translateY(-3px) scale(1)';
                }, 150);
            }
        }

        // Keyboard navigation
        document.addEventListener('keydown', (e) => {
            if (e.key === 'ArrowRight' || e.key === ' ') {
                e.preventDefault();
                changeSlide(1);
            } else if (e.key === 'ArrowLeft') {
                e.preventDefault();
                changeSlide(-1);
            }
        });

        // Touch/swipe support
        let startX = 0;
        let endX = 0;

        document.addEventListener('touchstart', (e) => {
            startX = e.touches[0].clientX;
        });

        document.addEventListener('touchend', (e) => {
            endX = e.changedTouches[0].clientX;
            handleSwipe();
        });

        function handleSwipe() {
            const swipeThreshold = 50;
            const diff = startX - endX;
            
            if (Math.abs(diff) > swipeThreshold) {
                if (diff > 0) {
                    changeSlide(1); // Swipe left - next slide
                } else {
                    changeSlide(-1); // Swipe right - previous slide
                }
            }
        }

        // Auto-advance (optional - uncomment to enable)
        /*
        setInterval(() => {
            if (currentSlideIndex < totalSlides - 1) {
                changeSlide(1);
            } else {
                currentSlideIndex = 0;
                updateSlide();
            }
        }, 10000); // 10 seconds
        */

        // Add hover effects to cards
        document.querySelectorAll('.marco-card, .impact-item, .right-item').forEach(card => {
            card.addEventListener('mouseenter', function() {
                this.style.transform = this.style.transform.replace('scale(1)', 'scale(1.05)');
                if (!this.style.transform.includes('scale')) {
                    this.style.transform += ' scale(1.05)';
                }
            });
            
            card.addEventListener('mouseleave', function() {
                this.style.transform = this.style.transform.replace('scale(1.05)', 'scale(1)');
            });
        });

        // Animate elements when they come into view
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '1';
                    entry.target.style.transform = 'translateY(0)';
                }
            });
        }, observerOptions);

        // Observe all animated elements
        document.querySelectorAll('.impact-item, .right-item, .marco-card').forEach(el => {
            observer.observe(el);
        });
    </script>
</body>
</html>
